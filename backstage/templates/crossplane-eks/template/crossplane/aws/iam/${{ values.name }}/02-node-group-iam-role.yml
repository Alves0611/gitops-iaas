apiVersion: iam.aws.upbound.io/v1beta1
kind: Role
metadata:
  name: ${{ values.name }}-node-group-role
  labels:
    environment: ${{ values.environment }}
    managed-by: crossplane
spec:
  forProvider:
    assumeRolePolicy: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Action": "sts:AssumeRole",
            "Effect": "Allow",
            "Principal": {"Service": "ec2.amazonaws.com"}
          }
        ]
      }
    tags:
      Name: ${{ values.name }}-node-group-role
      Environment: ${{ values.environment }}
      ManagedBy: crossplane
  providerConfigRef:
    name: default
---
apiVersion: iam.aws.upbound.io/v1beta1
kind: RolePolicyAttachment
metadata:
  name: ${{ values.name }}-node-group-eks-worker-node-policy
  labels:
    environment: ${{ values.environment }}
    managed-by: crossplane
spec:
  forProvider:
    policyArn: arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
    roleRef:
      name: ${{ values.name }}-node-group-role
  providerConfigRef:
    name: default
---
apiVersion: iam.aws.upbound.io/v1beta1
kind: RolePolicyAttachment
metadata:
  name: ${{ values.name }}-node-group-eks-cni-policy
  labels:
    environment: ${{ values.environment }}
    managed-by: crossplane
spec:
  forProvider:
    policyArn: arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
    roleRef:
      name: ${{ values.name }}-node-group-role
  providerConfigRef:
    name: default
---
apiVersion: iam.aws.upbound.io/v1beta1
kind: RolePolicyAttachment
metadata:
  name: ${{ values.name }}-node-group-ecr-read-only
  labels:
    environment: ${{ values.environment }}
    managed-by: crossplane
spec:
  forProvider:
    policyArn: arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
    roleRef:
      name: ${{ values.name }}-node-group-role
  providerConfigRef:
    name: default
---
apiVersion: iam.aws.upbound.io/v1beta1
kind: RolePolicyAttachment
metadata:
  name: ${{ values.name }}-node-group-ssm-managed-instance
  labels:
    environment: ${{ values.environment }}
    managed-by: crossplane
spec:
  forProvider:
    policyArn: arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
    roleRef:
      name: ${{ values.name }}-node-group-role
  providerConfigRef:
    name: default
